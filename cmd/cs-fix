#!/bin/bash
CONTAINER=$(cmd/container-name)
NAME="_phpfpm";
CONTAINER_NAME="$CONTAINER$NAME"
docker exec -it --user $(id -u):$(id -g) $CONTAINER_NAME vendor/bin/php-cs-fixer fix --allow-risky yes --show-progress dots --verbose